# Nested route
- As nested route indicam urls mais complexas como: /products/:id/something
- Neste caso vamoms precisar criar um componente que corresponda com o padrão indicado e também a url em App.js
- Na nested route teremos o acesso ao parâmetro da url também